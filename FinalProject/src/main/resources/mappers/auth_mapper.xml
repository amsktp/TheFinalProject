<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC
 "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.englishvillage.member.auth">

	<resultMap type="com.englishvillage.auth.model.MemberDto" id="memberResultMap">
		<id column="MEMBER_NO" property="memberNo"/>
		<result column="MEMBER_NAME" property="memberName" />
		<result column="MEMBER_EMAIL" property="memberEmail" />
		<result column="MEMBER_GRADE" property="memberGrade" />
		<result column="MEMBER_PASSWORD" property="memberPassword" />
	
	</resultMap>

	<select id="memberExist" parameterMap="map" resultMap="memberResultMap">
		SELECT MEMBER_NO, MEMBER_NAME, MEMBER_EMAIL, MEMBER_GRADE
		FROM MEMBER
		WHERE MEMBER_EMAIL = #{email}
		AND MEMBER_PASSWORD = #{password}
	</select>
	
</mapper>




